# Public Firebase configuration is supplied in apps/client/firebase.ts.
# Enable only while the local Auth, Firestore and Functions emulators are running.
VITE_USE_EMULATORS=false
# Optional native Blender executable for npm run blender:build.
# BLENDER_PATH=F:/Tools/Blender/blender.exe
